# Rename to backend.tf after creating the S3 bucket and DynamoDB table.
#
#   aws s3api create-bucket --bucket YOUR-TFSTATE-BUCKET --region us-east-1
#   aws dynamodb create-table \
#     --table-name terraform-locks-blacklist-svc \
#     --attribute-definitions AttributeName=LockID,AttributeType=S \
#     --key-schema AttributeName=LockID,KeyType=HASH \
#     --billing-mode PAY_PER_REQUEST
#
# terraform {
#   backend "s3" {
#     bucket         = "YOUR-TFSTATE-BUCKET"
#     key            = "dev/terraform.tfstate"
#     region         = "us-east-1"
#     dynamodb_table = "terraform-locks-blacklist-svc"
#     encrypt        = true
#   }
# }
